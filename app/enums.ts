export enum TrackStatus {
  DRAFT = "draft",
  PUBLISHED = "published",
  UNLISTED = "unlisted",
  HIDDEN = "hidden",
  DELETED = "deleted",
}

export enum AlbumStatus {
  PUBLISHED = "published",
  HIDDEN = "hidden",
  DELETED = "deleted",
}
